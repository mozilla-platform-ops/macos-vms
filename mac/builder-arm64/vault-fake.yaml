---
# Fake vault for PR/CI builds only. Contains no real credentials.
# Structure matches the flat Hiera key format produced by the hiera_vault backend.
# The real vault-real.yaml on the runner has identical keys with actual values.

vault_secrets::cltbld_user.data.password: "fake_cltbld_password_for_testing_only"
vault_secrets::cltbld_user.data.salt: "fake_salt_490238490238ijfklsdjfkljsdkljf"
vault_secrets::cltbld_user.data.iterations: "40000"
vault_secrets::cltbld_user.data.kcpassword: "fake_kcpassword_for_testing_only"

# These are shared for both roles in fake builds.
# Real vault has role-specific credentials. Both -vms roles use the existing
# datacenter-gecko-{1,3}-b-osx-arm64 clients (wildcard scopes cover the -vms pool suffix).
vault_secrets::generic_worker.data.taskcluster_client_id: "project/releng/generic-worker/datacenter-gecko-1-b-osx-arm64"
vault_secrets::generic_worker.data.taskcluster_access_token: "fake_access_token_for_testing_only"

vault_secrets::telegraf.data.user: "fake_telegraf_user"
vault_secrets::telegraf.data.password: "fake_telegraf_password_for_testing_only"
